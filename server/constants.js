const MAP_SIZE = {
  MIN: 0,
  MAX: 1000,
};

const TOWN_SIZE = {
  MIN: 60,
  MAX: 400,
};

const MARGIN = 20;

const RECYCLE_NUMBER = 10000;

const MAILBOX_SIZE = {
  MIN: 1,
  MAX: 10,
};

const COLOR_DELAY_TIME_MS = 2000;

const MAILBOX_PROBABILITY = 5;

export {
  MAP_SIZE,
  RECYCLE_NUMBER,
  TOWN_SIZE,
  MARGIN,
  MAILBOX_SIZE,
  MAILBOX_PROBABILITY,
  COLOR_DELAY_TIME_MS,
};
