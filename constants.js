const MAP_SIZE = {
  MIN: 0,
  MAX: 1000,
};

const TOWN_SIZE = {
  MIN: 60,
  MAX: 400,
};

const MARGIN = 20;

const RECYCLE_NUMBER = 10000;

export { MAP_SIZE, RECYCLE_NUMBER, TOWN_SIZE, MARGIN };
